{
  "version": 2,
  "buildCommand": "npm run db:generate && npm run build -w @storeflow/api",
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
